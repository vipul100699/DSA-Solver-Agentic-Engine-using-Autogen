# First, define a function add_to_numbers
def add_to_numbers(a: int, b: int) -> int:
    """
    This function adds two numbers.

    Args:
        a (int): The first number.
        b (int): The second number.

    Returns:
        int: The sum of the two numbers.
    """
    # Add a and b together
    result = a + b
    
    # Return the result
    return result

# Now, let's call the function with some test cases
print(add_to_numbers(3, 5))  # Expected output: 8
print(add_to_numbers(-5, 6))  # Expected output: 1
print(add_to_numbers(10, 2))  # Expected output: 12
